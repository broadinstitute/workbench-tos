
name: Code Coverage

on:
  push:
    branches: [ develop ]
  pull_request:
    branches: [ develop ]

jobs:
  coverage:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x]

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v1
      with:
        node-version: ${{ matrix.node-version }}
    - name: Install dependencies
      # if "npm ci" becomes too slow or unreliable, switch to caching dependencies
      # see https://docs.github.com/en/free-pro-team@latest/actions/guides/caching-dependencies-to-speed-up-workflows
      run: npm ci
      working-directory: function
    - name: Make coverage dirs
      run: mkdir .nyc_output
      working-directory: function
    - name: Calculate coverage
      run: ./node_modules/.bin/nyc report --reporter=text-lcov > test/coverage.lcov
      working-directory: function
    - name: Upload to codecov
      uses: codecov/codecov-action@v1
      with:
        fail_ci_if_error: false
        file: function/test/coverage.lcov
        verbose: false # optional (default = false)
